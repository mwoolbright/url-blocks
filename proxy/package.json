{
  "name": "proxy-pac-server",
  "version": "1.0.0",
  "description": "A simple proxy server that serves a PAC file and blocks specified domains with Let's Encrypt SSL support",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^3.0.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
